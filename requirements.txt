# 核心依赖（pip install -r requirements.txt）
# 完整可选依赖：pip install -e ".[rag,mysql-state,dev]"
langgraph>=0.1.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
langgraph-checkpoint-sqlite>=3.0.0

pymysql>=1.1.0
pymongo>=4.6.0
elasticsearch>=8.12.0

python-dotenv>=1.0.0
pydantic>=2.6.0
requests>=2.31.0
jsonschema>=4.20.0
loguru>=0.7.0
jieba>=0.42.1  # 默认 BM25 事故检索的中文分词（非向量专属）

fastapi>=0.110.0
uvicorn>=0.29.0
mcp>=1.2

# 数据源凭据加密（Fernet）
cryptography>=42.0.0
