[project]
name = "dataagent"
version = "1.0.0"
description = "基于 LangChain + LangGraph 的数仓多 Agent 协作系统（数据集成 / ETL / 运维）"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
